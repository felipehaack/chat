/*
 import {ChatWebPage} from './app.po'

 describe('Chat Web Page', () => {
 let page: ChatWebPage;

 beforeEach(() => {
 page = new ChatWebPage();
 });

 it('should display message saying app works', () => {
 page.navigateTo();
 expect(page.getParagraphText()).toEqual('app works!');
 });
 });
 */
